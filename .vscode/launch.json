{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "edge",
            "request": "launch",
            "name": "Launch Microsoft Edge on localhost",
            "webRoot": "${workspaceFolder}",
            "url": "http://localhost:3000"
        }
    ]
}